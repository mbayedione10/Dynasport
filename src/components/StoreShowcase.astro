---
import { createClient } from '@supabase/supabase-js';

const supabase = createClient(
  import.meta.env.PUBLIC_SUPABASE_URL,
  import.meta.env.PUBLIC_SUPABASE_ANON_KEY
);

// Récupération des images du showroom
const { data: showcaseImages, error } = await supabase
  .from('showcase_images')
  .select('*')
  .eq('active', true)
  .order('order', { ascending: true });

if (error) {
  console.error('Erreur lors de la récupération des images du showroom:', error);
}

const features = [
  {
    icon: '🎯',
    title: 'Expertise Pro',
    description: 'Conseillers experts issus du monde du sport'
  },
  {
    icon: '⚡',
    title: 'Service Express',
    description: 'Personnalisation en 24h pour les clubs'
  },
  {
    icon: '🏆',
    title: 'Qualité Premium',
    description: 'Sélection exclusive d\'équipements pro'
  }
];
---

<section class="py-24 bg-gradient-to-b from-white to-gray-50">
  <div class="container">
    <div class="text-center max-w-3xl mx-auto mb-16">
    <span class="inline-block px-4 py-2 bg-secondary/10 text-secondary rounded-full mb-4 text-sm font-bold animate-pulse">
        Nouveau • Ouverture Exceptionnelle
      </span>
      <h2 class="text-4xl md:text-5xl font-display font-bold mb-6 bg-gradient-to-r from-primary to-secondary bg-clip-text text-transparent">
        L'Excellence Sportive a son Adresse
      </h2>
      <p class="text-lg text-gray-600 leading-relaxed">
        Découvrez le plus grand showroom d'équipements sportifs professionnels de Paris. Un espace unique où technologie et expertise se rencontrent pour sublimer votre performance.
      </p>
      <a href="/contact" class="inline-flex items-center gap-2 mt-8 btn-primary group">
        <span>Planifier ma visite</span>
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 transform group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
        </svg>
      </a>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-16">
      {showcaseImages?.map(image => (
        <div class="bg-white rounded-xl overflow-hidden shadow-lg group hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
          <div class="relative h-80">
            <img
              src={image.image_url}
              alt={image.title}
              class="w-full h-full object-cover transform group-hover:scale-105 transition-transform duration-500"
              loading="lazy"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300">
              <div class="absolute bottom-0 left-0 right-0 p-6 transform translate-y-4 group-hover:translate-y-0 transition-transform">
                <h3 class="text-xl font-bold mb-2 text-white">{image.title}</h3>
                <p class="text-white/90">{image.description}</p>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-16">
      {features.map(feature => (
        <div class="bg-white p-8 rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
          <div class="text-4xl mb-4">{feature.icon}</div>
          <h3 class="text-xl font-bold mb-2 text-primary">{feature.title}</h3>
          <p class="text-gray-600">{feature.description}</p>
        </div>
      ))}
    </div>

    <div class="bg-white p-8 rounded-xl shadow-lg text-center">
      <div class="inline-flex items-center gap-3 text-primary mb-4">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
        </svg>
        <span class="font-bold text-lg">123 Avenue des Sports, 75001 Paris</span>
      </div>
      <div class="flex flex-wrap justify-center gap-8 text-gray-600">
        <div class="flex items-center gap-2">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <span>Lun-Sam: 10h-19h</span>
        </div>
        <div class="flex items-center gap-2">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
          </svg>
          <span>01 23 45 67 89</span>
        </div>
        <div class="flex items-center gap-2">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
          </svg>
          <span>Ouvert 7j/7 pour les clubs</span>
        </div>
      </div>
    </div>
  </div>
</section>